create-incident-combined-file-one:
  - create incident from combined file:
      create-inc:
        name: Multi Scenario Incident One
  - validate:
      id:
        exists: true
      name: Multi Scenario Incident One

create-incident-combined-file-two:
  - create second incident from combined file:
      create-inc:
        name: Multi Scenario Incident Two
  - validate:
      id:
        exists: true
      name: Multi Scenario Incident Two
