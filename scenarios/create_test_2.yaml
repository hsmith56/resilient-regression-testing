create-incident-standalone-file-two:
  - create incident from second standalone file:
      create-inc:
        name: Second Standalone Scenario Incident
  - validate:
      id:
        exists: true
      name: Second Standalone Scenario Incident
