create-incident-standalone-file-one:
  - create incident from first standalone file:
      create-inc:
        name: First Standalone Scenario Incident
  - validate:
      id:
        exists: true
      name: First Standalone Scenario Incident
